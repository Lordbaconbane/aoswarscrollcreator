// Pascal Casing. React uses this convention
function Message() {
  // JSX: Javascript XMl
  const name = "Mozzarella";

  if (name) return <h1>{name}</h1>;
  return <h1> Hello world! </h1>;
}

export default Message;
